{{#each @row as |mark index|}}
  <BoardCell @mark={{mark}}
             @index={{index}}
             @winner={{@winner}}
             @store={{action store this.index}} as |result|>
    {{ result }}
  </BoardCell>
{{/each}}
